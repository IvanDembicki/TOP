# ModeRouter

Purpose: select the active mode for the skill.

Rules:
- activate `CreateLaunchPlanMode` only when minimum launch inputs are sufficient
- otherwise route to clarification before mode completion