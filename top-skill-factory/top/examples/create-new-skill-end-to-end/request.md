# Original request

Create a small TOP-based skill that helps a user build a short execution plan from a goal.

Requirements:
- one main mode
- one clarification path when the goal is underspecified
- validation rules that prevent ready state when required files are missing
- no legacy import path