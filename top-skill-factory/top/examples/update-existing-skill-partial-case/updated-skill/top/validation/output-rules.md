# Output validation rules

Checks:
- updated mode-router exists
- concise mode still exists
- detailed mode exists
- update scope artifacts exist
- example set must eventually reflect the new optional mode

Blocking violations:
- required update artifacts are missing
- detailed mode silently becomes the default behavior

Non-blocking but acceptance-relevant gaps:
- example set for the detailed mode is not yet updated