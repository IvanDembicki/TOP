# CreateDetailedChecklistMode

Purpose: produce a more detailed execution checklist when explicitly requested.

Rules:
- do not activate unless the user clearly asks for more detail
- preserve bounded, step-oriented output
- do not replace the concise path as default behavior