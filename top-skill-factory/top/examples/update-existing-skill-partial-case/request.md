# Original update request

Update the existing plan-builder skill so that it can offer an optional "detailed execution checklist" mode.

Constraints:
- keep the old concise output behavior intact
- preserve existing validation rules
- avoid changing the public behavior unless the user explicitly asks for the detailed mode
- examples can be updated later if needed