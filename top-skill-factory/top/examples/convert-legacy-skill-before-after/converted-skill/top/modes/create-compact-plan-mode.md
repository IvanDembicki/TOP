# CreateCompactPlanMode

Purpose: return a concise markdown plan for a sufficiently specified request.

Input:
- normalized user goal
- resolved clarification answers when needed

Output:
- compact markdown plan

Rules:
- prioritize practical next steps
- keep structure lightweight and scannable
- do not add strategic risk structure unless the request was explicitly routed for depth
