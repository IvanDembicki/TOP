# RollbackMode End-to-End Example

This example demonstrates a bounded rollback flow.

The rollback returns from a riskier current variant to a previously validated target version.

It includes:
- rollback request
- structured rollback input
- version history
- rollback record
- rollback plan
- restored skill bundle
- rollback audit
- final decision