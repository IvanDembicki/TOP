# Plan Builder Skill (Restored)

This restored variant returns to the previously validated concise-default behavior.

Current guarantees:
- concise mode is the default path
- detailed checklist mode activates only on explicit request
- rollback reason is preserved in audit artifacts